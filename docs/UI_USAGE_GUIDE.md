# Руководство по пользовательскому интерфейсу

## 🎯 Обзор системы вывода

Stencil Analyzer использует двухуровневую систему вывода информации для четкого разделения сообщений для оператора и технической информации.

### Два уровня вывода:

1. **UIService** - рабочий интерфейс (БЕЛЫЙ цвет)
   - Основные сообщения для оператора
   - Статусы выполнения операций
   - Результаты обработки
   - Инструкции для пользователя

2. **DebugFormatter** - отладочная информация (СЕРЫЙ цвет)  
   - Технические детали обработки
   - Диагностическая информация
   - Статистика и метрики
   - Отладочные данные

## 🏗️ Архитектура системы вывода

### Базовые компоненты:

- **LoggingService** - инфраструктура логирования
- **UIService** - пользовательский интерфейс (белый цвет)
- **DebugFormatter** - отладочный вывод (серый цвет)

### Цветовое кодирование:

```python
# UIService - сообщения для оператора (БЕЛЫЙ)
self._log_extra = {"className": self.__class__.__name__}

# DebugFormatter - техническая информация (СЕРЫЙ)  
self._log_extra = {
    "className": self.__class__.__name__,
    "is_debug_formatter": True  # ← Активирует серый цвет
}
```

## 📝 Правила форматирования

### 1. Структура сообщений

**Правило:** Префикс всегда в колонке 0, отступы применяются только к тексту.

```python
# ✅ ПРАВИЛЬНО
def show_message(self, text: str, indent: int = 0):
    prefix = "[INFO] "
    indent_str = " " * (indent * 2)
    message = f"{prefix} {indent_str}{text}"
    # Результат: "[INFO]   текст с отступом"
    #            ^^^^^^^   ^^^^^^^^^^^^^^^^
    #            колонка 0  отступ применен

# ❌ НЕПРАВИЛЬНО
def show_message_wrong(self, text: str, indent: int = 0):
    prefix = "[INFO] "
    indent_str = " " * (indent * 2)
    message = f"{indent_str}{prefix} {text}"
    # Результат: "  [INFO]  текст"
    #            ^^^^^^^^^^  ^^^^
    #            префикс сдвинут - нарушение стандарта!
```

### 2. Типы элементов вывода

#### Сообщения с префиксами (для обоих сервисов):
- Информационные сообщения
- Статусы выполнения
- Ошибки и предупреждения
- Результаты операций

#### Структурные элементы (БЕЗ префиксов):
- Заголовки и разделители
- Таблицы и блоки данных
- Визуальные разделители
- Прогресс-бары

## 🎨 Примеры использования

### UIService - интерфейс оператора

```python
ui = UIService(config)

# Основные сообщения
ui.show_message("Начало обработки заказа")        # [INFO]  Начало обработки заказа
ui.show_success("Файл успешно загружен")          # [OK]    Файл успешно загружен  
ui.show_error("Ошибка чтения файла", indent=1)    # [ERROR]   Ошибка чтения файла
ui.show_warning("Проверьте ориентацию")           # [WARN]  Проверьте ориентацию

# Структурные элементы
ui.show_header()                                  # СИСТЕМА КОНТРОЛЯ КАЧЕСТВА...
ui.show_main_stage("ОБРАБОТКА ИЗОБРАЖЕНИЙ")      # [2/6] ОБРАБОТКА ИЗОБРАЖЕНИЙ
ui.separator()                                    # ──────────────────────────

# Блоки данных
ui.show_compact_block("Параметры платы:", [
    ("Номер заказа", "090247"),
    ("Размер", "100 × 50 мм"),
    ("Оператор", "Иванов И.И.")
])
```

### DebugFormatter - техническая информация

```python
dbg = DebugFormatter(config, __name__)

# Технические сообщения
dbg.info("Начало анализа изображения")            # [INFO]  Начало анализа изображения
dbg.debug("Размер: 1920×1080", indent=1)          # [DEBUG]   Размер: 1920×1080
dbg.success("Анализ завершен")                    # [OK]    Анализ завершен

# Диагностические блоки
dbg.header("ДИАГНОСТИКА СОВМЕЩЕНИЯ")              # ═════════════════════════
dbg.section("Оценка стратегий бинаризации")       # → Оценка стратегий бинаризации
dbg.separator()                                   # ──────────────────────────

# Таблицы сравнения стратегий
dbg.strategy_table_start("BINARIZATION", 3)
dbg.strategy_table_row("Otsu Strategy", 0.85, 0.12, True)
dbg.strategy_table_row("Adaptive Mean", 0.72, 0.15, False)
dbg.strategy_table_end()

# Вложенные блоки
with dbg.indent(2):
    dbg.info("Детальный анализ контуров")         # [INFO]      Детальный анализ контуров
    dbg.debug("Найдено 42 контура")               # [DEBUG]       Найдено 42 контура
```

## 📊 Визуальные примеры вывода

### Типичная сессия обработки:

```
СИСТЕМА КОНТРОЛЯ КАЧЕСТВА ТРАФАРЕТОВ v2.1.0        ← белый, без префикса (UIService)

[1/6] ПОДГОТОВКА ДАННЫХ                            ← белый, без префикса (UIService)
────────────────────────────────────────           ← белый, без префикса (UIService)

[INFO]  Поиск Gerber-файла для заказа 090247       ← белый, с префиксом (UIService)
[OK]    Файл найден: 090247.gbr                   ← белый, с префиксом (UIService)
[INFO]  Ожидание скана в папке test_scan/          ← белый, с префиксом (UIService)
[OK]    Скан обнаружен: 090247.jpg                ← белый, с префиксом (UIService)

[DEBUG] Начало обработки изображений               ← серый, с префиксом (DebugFormatter)
═══════════════════════════════════════            ← серый, без префикса (DebugFormatter)
        АНАЛИЗ КАЧЕСТВА СОВМЕЩЕНИЯ                 ← серый, без префикса (DebugFormatter)
═══════════════════════════════════════            ← серый, без префикса (DebugFormatter)
[INFO]  Корреляция: 0.854                          ← серый, с префиксом (DebugFormatter)
[OK]    Статус: УСПЕШНО                           ← серый, с префиксом (DebugFormatter)

[INFO]  Результаты сохранены в test_results/090247/ ← белый, с префиксом (UIService)
[OK]    Обработка завершена успешно                ← белый, с префиксом (UIService)
```

## 🔧 Рекомендации по использованию

### Когда использовать UIService:
- ✅ Сообщения для конечного пользователя
- ✅ Статусы выполнения операций
- ✅ Результаты обработки
- ✅ Инструкции и подсказки
- ✅ Ошибки, требующие действий пользователя

### Когда использовать DebugFormatter:
- ✅ Технические детали реализации
- ✅ Диагностическая информация
- ✅ Статистика производительности
- ✅ Отладочные данные
- ✅ Внутренние метрики качества

### Сообщения для режима отладки:
```python
# В обработчиках изображений
if self.config_service.debug_mode:
    dbg.info(f"Размер изображения: {image.shape}")
    dbg.debug(f"Параметры обработки: {params}")

# Всегда показываем пользователю
ui.show_success("Обработка завершена")
```

## 🚀 Best Practices

### 1. Четкое разделение ответственности
```python
# ✅ ХОРОШО
ui.show_message("Начало обработки")      # Для пользователя
dbg.debug("Параметры: " + str(params))   # Для разработчика

# ❌ ПЛОХО
ui.show_message(f"DEBUG: {debug_data}")  # Техническая информация в UI
```

### 2. Правильные отступы
```python
# ✅ ХОРОШО
ui.show_message("Основной процесс")
ui.show_message("Детали", indent=1)      # [INFO]   Детали
ui.show_message("Подробности", indent=2) # [INFO]     Подробности

# ❌ ПЛОХО  
ui.show_message("  Детали")              # Отступ в тексте - сложно поддерживать
```

### 3. Структурирование вывода
```python
# ✅ ХОРОШО - четкая структура
ui.show_main_stage("ОБРАБОТКА ИЗОБРАЖЕНИЙ")
ui.separator()
ui.show_message("Загрузка изображения")
ui.show_message("Предварительная обработка", indent=1)
ui.show_success("Обработка завершена")

# ❌ ПЛОХО - плохая структура
ui.show_message("ОБРАБОТКА ИЗОБРАЖЕНИЙ")
ui.show_message("Загрузка изображения")
ui.show_message("Предварительная обработка")
ui.show_message("Обработка завершена")
```

## 🐛 Решение проблем

### Проблема: Неправильные цвета вывода
**Симптомы:** Сообщения UIService выводятся серым цветом

**Решение:** Проверить что в UIService НЕ установлен `is_debug_formatter`:
```python
# ✅ ПРАВИЛЬНО (UIService)
self._log_extra = {"className": self.__class__.__name__}

# ❌ НЕПРАВИЛЬНО (UIService)
self._log_extra = {
    "className": self.__class__.__name__,
    "is_debug_formatter": True  # ← УБРАТЬ!
}
```

### Проблема: Префиксы в структурных элементах
**Симптомы:** Заголовки и разделители имеют префиксы [INFO]

**Решение:** Использовать `is_preformatted=True` для элементов без префиксов:
```python
# ✅ ПРАВИЛЬНО
log_extra = {**self._log_extra, "is_preformatted": True}
self.logger.info(header_text, extra=log_extra)
```

### Проблема: Неправильные отступы
**Симптомы:** Префиксы сдвинуты от левого края

**Решение:** Применять отступы только к тексту, не к префиксам:
```python
# ✅ ПРАВИЛЬНО
message = f"{prefix} {indent_str}{text}"

# ❌ НЕПРАВИЛЬНО
message = f"{indent_str}{prefix} {text}"
```

---

## 📋 Чек-лист для разработчиков

### При работе с UIService:
- [ ] Использовать для сообщений пользователю
- [ ] НЕ добавлять `is_debug_formatter` 
- [ ] Применять отступы через параметр `indent`
- [ ] Использовать `_log_without_prefix()` для заголовков
- [ ] Сообщения должны быть понятны конечному пользователю

### При работе с DebugFormatter:
- [ ] Использовать для технической информации
- [ ] Убедиться что `is_debug_formatter=True`
- [ ] Использовать для диагностики и отладки
- [ ] Сообщения могут содержать технические детали
- [ ] Активно использовать в режиме отладки

### Общие правила:
- [ ] Префиксы всегда в колонке 0
- [ ] Отступы только для текста сообщения
- [ ] Структурные элементы без префиксов
- [ ] Четкое разделение пользовательских и технических сообщений
- [ ] Консистентность стиля во всем приложении

---

*Руководство по пользовательскому интерфейсу v2.1.0*  
*Для внутреннего использования разработчиками*