# Руководство по эксплуатации и поддержке

## 🎯 Общие сведения

Stencil Analyzer - система автоматического контроля качества PCB трафаретов. Приложение сравнивает эталонные Gerber-файлы с отсканированными изображениями трафаретов и оценивает качество совмещения.

### Основные характеристики:

- **Точность:** оценка совмещения с точностью до 0.001 мм
- **Производительность:** обработка одного скана за 10-60 секунд
- **Поддержка форматов:** Gerber (.gbr), изображения (.jpg, .png, .tiff, .bmp)
- **Автоматизация:** интеллектуальный выбор алгоритмов обработки

## 📋 Ежедневная эксплуатация

### Стандартный рабочий процесс

#### 1. Подготовка файлов

```
v13_final/
├── test_gerbers/    # ⬅ Поместить Gerber-файлы
│   └── 090247.gbr   #    (имя содержит номер заказа)
├── test_scan/       # ⬅ Поместить сканы трафаретов
│   └── 090247.jpg   #    (имя содержит номер заказа)
└── test_results/    # ⬅ Результаты (создается автоматически)
```

**Требования к файлам:**

- **Gerber-файлы:** должны содержать 6-значный номер заказа в имени
- **Сканы:** разрешение 1200 DPI, равномерное освещение, правильная ориентация
- **Соответствие:** номера заказов должны совпадать в именах файлов

#### 2. Запуск приложения

```cmd
# Рекомендуемый способ (режим отладки)
run.bat

# Альтернативный способ
python main.py --debug
```

#### 3. Ввод данных

```
Введите ФИО оператора: Иванов И.И.
Введите номер заказа (6 цифр): 090247
```

#### 4. Мониторинг процесса обработки

Приложение отображает прогресс выполнения:

```
[INFO]  Поиск Gerber-файла для заказа 090247...
[OK]    Файл найден: 090247.gbr
[INFO]  Ожидание скана в папке test_scan/...
[OK]    Скан обнаружен: 090247.jpg
[INFO]  Фаза 1: Подготовка данных...
[INFO]  Фаза 2: Анализ Gerber-файла...
[INFO]  Фаза 3: Обработка изображения...
[OK]    Обработка завершена успешно
```

**Время обработки:**

- Малые файлы (<10 МБ): 10-20 секунд
- Средние файлы (10-50 МБ): 20-40 секунд
- Крупные файлы (>50 МБ): 40-60 секунд

#### 5. Анализ результатов

##### Папка с результатами:

```
test_results/090247/
├── 090247_1_gerber.png      # Эталон из Gerber
├── 090247_2_scan.png        # Исходный скан
├── 090247_4_compared.png    # Визуальное сравнение ✅
├── 090247_6_log_short.txt   # Краткий отчет ✅
└── application.log          # Общий лог приложения
```

##### Интерпретация визуального сравнения (compared.png):

- **🔴 Красный:** эталон (Gerber)
- **🔵 Голубой:** скан трафарета
- **⚪ Белый:** области совпадения
- **⚫ Черный:** области несовпадения

**Идеальный результат:** равномерное белое наложение по всей площади.

##### Анализ текстового отчета:

```log
Дата и время: 2024-12-05 14:30:25
Оператор: Иванов И.И.
Номер заказа: 090247
Результат корреляции: 0.854
Статус совмещения: УСПЕШНО
```

### Критерии оценки качества

| Статус                  | Корреляция | Визуальная оценка        | Рекомендуемое действие         |
| ----------------------- | ---------- | ------------------------ | ------------------------------ |
| ✅ **УСПЕШНО**          | 0.8 - 1.0  | Отличное совмещение      | Трафарет годен к использованию |
| ⚠ **С ПРЕДУПРЕЖДЕНИЕМ** | 0.4 - 0.8  | Частичное совмещение     | Требует визуальной проверки    |
| ❌ **НЕУДАЧНО**         | 0.0 - 0.4  | Значительные расхождения | Необходимо пересканировать     |

## 🔧 Техническое обслуживание

### Регулярные операции

#### Ежедневно:

- Проверка свободного места на диске (минимум 1 ГБ)
- Очистка папки `test_results/` от устаревших данных
- Проверка актуальности конфигурации `config.ini`

#### Еженедельно:

- Резервное копирование важных результатов
- Проверка целостности установки Python и библиотек
- Обновление логов приложения

#### Ежемесячно:

- Полная очистка системы от временных файлов
- Проверка производительности обработки
- Анализ статистики успешных обработок

### Очистка системы

```cmd
# Автоматическая очистка результатов
python clean.py

# Ручная очистка логов
del test_results\application.log
```

### Резервное копирование

**Критичные для резервирования данные:**

- Папка `test_results/` с результатами анализа
- Файл `config.ini` с настройками приложения
- База данных операций (если используется)

**Рекомендуемая частота:** ежедневное резервное копирование

## 🐛 Решение проблем

### Типовые проблемы и решения

#### Проблема 1: "Файлы не найдены"

```
❌ Ошибка: Gerber-файл для заказа 090247 не найден
```

**Причины:**

- Неправильный номер заказа в имени файла
- Файлы в неправильных папках
- Неправильные расширения файлов

**Решение:**

1. Проверить соответствие номеров заказов в именах файлов
2. Убедиться что файлы в папках `test_gerbers/` и `test_scan/`
3. Проверить расширения: `.gbr` для Gerber, `.jpg/.png` для сканов

#### Проблема 2: "Низкое качество совмещения" (корреляция < 0.4)

**Причины:**

- Низкое разрешение сканирования
- Неправильная ориентация трафарета
- Плохое освещение при сканировании
- Загрязнение или повреждение трафарета

**Решение:**

1. Использовать разрешение 1200 DPI при сканировании
2. Проверить правильность ориентации трафарета
3. Обеспечить равномерное освещение без бликов
4. Очистить трафарет от пыли и загрязнений

#### Проблема 3: "Ошибка обработки изображения"

**Причины:**

- Поврежденные файлы изображений
- Неподдерживаемый формат файла
- Недостаточно памяти для обработки

**Решение:**

1. Проверить целостность файлов изображений
2. Убедиться в использовании поддерживаемых форматов
3. Закрыть другие приложения для освобождения памяти

#### Проблема 4: "Медленная обработка"

**Причины:**

- Крупные файлы изображений
- Недостаточно ресурсов системы
- Много параллельных процессов

**Решение:**

1. Оптимизировать размер сканов (рекомендуется 100-300 DPI для больших файлов)
2. Закрыть ненужные приложения во время обработки
3. Увеличить таймауты в `config.ini` при необходимости

### Диагностика проблем

#### Использование режима отладки:

```cmd
run.bat
```

**Преимущества режима отладки:**

- Подробные сообщения о каждом этапе обработки
- Сохранение промежуточных изображений
- Детальная статистика производительности
- Расширенные логи для технической поддержки

#### Анализ логов:

```
test_results/application.log          # Основной лог приложения
test_results/090247/090247_6_log_short.txt  # Лог конкретного заказа
```

**Ключевые элементы для диагностики:**

- Время выполнения каждого этапа
- Использованные алгоритмы обработки
- Детальные метрики качества
- Сообщения об ошибках

## ⚙️ Настройка и конфигурация

### Основные параметры config.ini

#### Производительность:

```ini
[SYSTEM]
max_scan_file_size = 100          ; Максимальный размер файла (МБ)
scan_wait_timeout = 300           ; Таймаут ожидания скана (секунды)

[SCANNING]
file_check_interval = 1           ; Интервал проверки файлов (секунды)
```

#### Качество обработки:

```ini
[IMAGE_COMPARISON]
rotation_angles = 0,90,180,270    ; Углы для проверки совмещения
low_correlation_threshold = 0.2   ; Порог "неудачно"
medium_correlation_threshold = 0.4 ; Порог "с предупреждением"
high_correlation_threshold = 0.8  ; Порог "успешно"
```

#### Визуализация:

```ini
[VISUALIZATION]
reference_color_r = 255           ; Цвет эталона (красный)
scan_color_g = 255                ; Цвет скана (голубой)
info_font_size = 20               ; Размер шрифта в отчетах
```

### Оптимизация для различных сценариев

#### Высокая точность (рекомендуется для критичных применений):

```ini
[IMAGE_COMPARISON]
rotation_angles = 0,45,90,135,180,225,270,315
ransac_reprojection_threshold = 1.0
max_iterations = 5000
```

#### Быстрая обработка (для серийной проверки):

```ini
[IMAGE_COMPARISON]
rotation_angles = 0,90,180,270
ransac_reprojection_threshold = 5.0
max_iterations = 1000
```

## 📈 Мониторинг и отчетность

### Ключевые метрики для мониторинга

#### Производительность:

- Среднее время обработки одного заказа
- Количество обработанных заказов в час/смену
- Процент успешных обработок

#### Качество:

- Средняя корреляция совмещения
- Распределение статусов (успешно/предупреждение/неудачно)
- Частота возникновения ошибок

### Статистика использования

#### Ежедневная статистика:

```python
# Из логов приложения
Обработано заказов: 24
Успешных: 22 (91.7%)
С предупреждением: 2 (8.3%)
Неудачных: 0 (0.0%)
Среднее время обработки: 28.5 секунд
```

#### Долгосрочная статистика:

- Тренды качества совмещения
- Эффективность различных алгоритмов
- Загрузка системы по времени суток

## 🔒 Безопасность и доступ

### Управление доступом

#### Права доступа к папкам:

- `test_gerbers/` - чтение для операторов
- `test_scan/` - чтение/запись для операторов
- `test_results/` - чтение для операторов, запись для системы
- `config.ini` - чтение для всех, запись для администраторов

### Рекомендации по безопасности

1. **Регулярное обновление** зависимостей Python
2. **Ограничение сетевого доступа** к рабочей станции
3. **Резервное копирование** конфигурации и результатов
4. **Мониторинг использования** дискового пространства

## 📞 Поддержка и контакты

### Внутренняя поддержка

#### Первый уровень (операторы):

- Решение типовых проблем эксплуатации
- Консультации по подготовке файлов
- Настройка параметров обработки

#### Второй уровень (технические специалисты):

- Диагностика сложных проблем
- Настройка и оптимизация конфигурации
- Обновление и техническое обслуживание

### Документация для обращения в поддержку

При обращении за технической поддержкой подготовьте:

1. **Файлы для воспроизведения проблемы:**

   - Gerber-файл и скан вызвавшие проблему
   - Файл конфигурации `config.ini`

2. **Диагностическая информация:**

   - Логи из `test_results/application.log`
   - Отчеты из папки проблемного заказа
   - Скриншот сообщения об ошибке

3. **Информация о системе:**
   - Версия Python (`python --version`)
   - Версия приложения (`python main.py --version`)
   - Характеристики рабочей станции

---

_Руководство по эксплуатации v2.1.0_  
_Для внутреннего использования_
